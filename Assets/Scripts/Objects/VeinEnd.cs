using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class VeinEnd
{
    public enum EndType
    {
        Sphere,
        None,
    }

    static void CreateEnd(Vector3 position, Quaternion rotation, EndType type, bool is_tunnel)
    {

    }
}
